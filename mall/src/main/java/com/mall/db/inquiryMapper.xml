<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="inquiry">

  <select id="totBoard" resultType="java.lang.Integer">
  	select count(*) FROM tb_cs
  </select>

  <select id="findAll" resultType="com.mall.vo.user.UserVO">
    select * from tb_member
  </select>
  
  <insert id="insertInquiry">
    	INSERT INTO tb_cs VALUES(seq_tb_cs.nextval,#{cs_mem_id},#{cs_title},#{cs_content},sysdate,#{cs_category_one},#{cs_category_two},#{cs_fname},#{cs_email},#{cs_phone})
  </insert>
  
  <select id="findAllInquiry" resultType="com.mall.vo.inquiry.InquiryVo">
      select cs_no, cs_title,cs_content,cs_regdate,cs_category_one,cs_category_two,cs_fname,cs_email,cs_phone from (select rownum num, cs_no, cs_title,cs_content,cs_regdate,cs_category_one,cs_category_two,cs_fname,cs_email,cs_phone from
	(select * from tb_cs order by cs_no desc)) where num between #{start} and #{end}
  </select>
  
  <select id="selectInquiry" resultType="com.mall.vo.inquiry.InquiryVo">
  		SELECT * FROM tb_cs WHERE cs_no = #{cs_no}
  </select>
  
</mapper>